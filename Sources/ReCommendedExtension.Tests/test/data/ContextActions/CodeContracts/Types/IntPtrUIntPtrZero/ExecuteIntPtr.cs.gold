using System;
using System.Diagnostics.Contracts;

namespace Test
{
    internal class ExecuteIntPtr
    {
        void Method(IntPtr one{caret})
        {
          Contract.Requires(one == IntPtr.Zero);
        }
    }
}