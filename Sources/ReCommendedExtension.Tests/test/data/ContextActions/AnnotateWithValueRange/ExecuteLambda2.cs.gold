using System;
using JetBrains.Annotations;

namespace Test
{
    internal class Types
    {
        void Method()
        {
            Action<int> lambda = ([ValueRange({caret})] first) => { };
        }
    }
}