using System.IO;
using System.Threading.Tasks;

namespace Test
{
    internal class Execute
    {
        async Task AsyncDisposable_Variable()
        {
          Stream m = new MemoryStream();
          aw{caret}ait using (m.ConfigureAwait(false)) { }
        }
    }
}