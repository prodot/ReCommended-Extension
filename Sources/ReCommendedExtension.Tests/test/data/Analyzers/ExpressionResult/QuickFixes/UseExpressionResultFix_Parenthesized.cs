using System;

namespace Test
{
    public class ExpressionResults
    {
        public void ExpressionResult(int number)
        {
            var result = int.RotateLeft{caret}(number + 1, 0).ToString();
        }
    }
}