using System;

namespace Test
{
    public class ExpressionResults
    {
        public void ExpressionResult(Random random, int[] array)
        {
            var result = |random.GetItems(array, 0)|(0);
        }

        public void ExpressionResult<T>(Random random, T[] array)
        {
            var result = |random.GetItems(array, 0)|(1);
        }
    }
}
------------------------------------------------
0: The expression is always an empty array.
QUICKFIXES:
Replace with 'Array.Empty<int>()'
1: The expression is always an empty array.
QUICKFIXES:
Replace with 'Array.Empty<T>()'
