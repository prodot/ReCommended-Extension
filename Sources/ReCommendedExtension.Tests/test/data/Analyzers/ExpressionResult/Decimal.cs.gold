namespace Test
{
    public class ExpressionResults
    {
        public void ExpressionResult(decimal number)
        {
            var result11 = |number.Equals(null)|(0);

            var result21 = |decimal.Clamp(number, 1, 1)|(1);
            var result22 = |decimal.Clamp(number, decimal.MinValue, decimal.MaxValue)|(2);

            var result31 = |number.GetTypeCode()|(3);

            var result41 = |decimal.Max(1, 1)|(4);
            var result42 = |decimal.Min(1, 1)|(5);
        }

        public void NoDetection(decimal number, decimal min, decimal max)
        {
            var result11 = decimal.Clamp(number, min, max);

            number.Equals(null);

            decimal.Clamp(number, 1, 1);
            decimal.Clamp(number, decimal.MinValue, decimal.MaxValue);

            number.GetTypeCode();

            decimal.Max(1, 1);
            decimal.Min(1, 1);
        }
    }
}
---------------------------------------------------------
(0): ReSharper Suggestion: The expression is always false.
(1): ReSharper Suggestion: The expression is always the same as the second or third argument.
(2): ReSharper Suggestion: The expression is always the same as the first argument.
(3): ReSharper Suggestion: The expression is always a TypeCode constant.
(4): ReSharper Suggestion: The expression is always the same as the first or second argument.
(5): ReSharper Suggestion: The expression is always the same as the first or second argument.
