using System;

namespace Test
{
    internal static class Class
    {
        static void Foo(string? x)
        {
            if (x != null)
            {
                Console.WriteLine(x|!|(0).Length);
            }
        }
    }
}
------------------------------------------------
0: Null-forgiving operator is redundant because the expression is never null.
QUICKFIXES:
Remove null-forgiving operator
