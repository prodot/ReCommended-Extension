using System.Collections.Generic;
using System.Linq;

namespace Test
{
    public class Class
    {
        public void Method(IList<int> list)
        {
            var f = (list is [.., var last] ? last : throw new InvalidOperationException{caret}("List is empty.")) switch { _ => 3 };
        }
    }
}