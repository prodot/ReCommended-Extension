using System.Collections.Generic;
using System.Linq;

namespace Test
{
    public class Class
    {
        public void Method(IList<int> list)
        {
            var f = list is [var first, ..] ? first : throw new InvalidOperationException("List is emp{caret}ty.");
        }
    }
}