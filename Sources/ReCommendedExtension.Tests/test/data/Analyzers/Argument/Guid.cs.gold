using System;

namespace Test
{
    public class Arguments
    {
        public void RedundantArgument(string s, ReadOnlySpan<char> s1, ReadOnlySpan<byte> utf8Text, IFormatProvider provider, out Guid result)
        {
            var result11 = Guid.Parse(s, |provider|(0));
            var result12 = Guid.Parse(s1, |provider|(1));
            var result13 = Guid.Parse(utf8Text, |provider|(2));

            var result21 = Guid.TryParse(s, |provider|(3), out result);
            var result22 = Guid.TryParse(s1, |provider|(4), out result);
            var result23 = Guid.TryParse(utf8Text, |provider|(5), out result);
        }

        public void NoDetection()
        {
            
        }
    }
}
---------------------------------------------------------
(0): ReSharper Dead Code: Passing a format provider is redundant.
(1): ReSharper Dead Code: Passing a format provider is redundant.
(2): ReSharper Dead Code: Passing a format provider is redundant.
(3): ReSharper Dead Code: Passing a format provider is redundant.
(4): ReSharper Dead Code: Passing a format provider is redundant.
(5): ReSharper Dead Code: Passing a format provider is redundant.
