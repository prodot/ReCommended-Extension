using System;

namespace Test
{
    public class Strings
    {
        public void RedundantInvocation(string text, int startIndex)
        {
            var result1 = text.Remove{caret}(startIndex, 0);
        }
    }
}