using System;

namespace Test
{
    public class Strings
    {
        public void Comparison(string text, char c)
        {
            var result11 = text is [var firstChar, ..] && {caret}firstChar == c;
        }
    }
}