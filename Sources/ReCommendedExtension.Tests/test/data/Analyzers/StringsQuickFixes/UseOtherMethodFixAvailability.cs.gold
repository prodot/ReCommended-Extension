using System;

namespace Test
{
    public class Strings
    {
        public void IndexOf(string text, char c)
        {
            var result31 = text.|IndexOf(c) > -1|(0);
            var result32 = text.|IndexOf(c) != -1|(1);
            var result33 = text.|IndexOf(c) >= 0|(2);

            var result41 = text.|IndexOf(c) == -1|(3);
            var result42 = text.|IndexOf(c) < 0|(4);
        }
    }
}
------------------------------------------------
0: Use the 'Contains' method
QUICKFIXES:
Replace with 'Contains(c)'
1: Use the 'Contains' method
QUICKFIXES:
Replace with 'Contains(c)'
2: Use the 'Contains' method
QUICKFIXES:
Replace with 'Contains(c)'
3: Use the 'Contains' method
QUICKFIXES:
Replace with '!Contains(c)'
4: Use the 'Contains' method
QUICKFIXES:
Replace with '!Contains(c)'
