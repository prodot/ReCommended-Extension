using System;

namespace Test
{
    public class Strings
    {
        public void Comparison(string text, char c)
        {
            var result21 = text is not [var firstCharacter, ..] || {caret}firstCharacter != c;
        }
    }
}