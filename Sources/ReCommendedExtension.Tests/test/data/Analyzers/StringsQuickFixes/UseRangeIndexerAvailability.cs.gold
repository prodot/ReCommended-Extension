using System;

namespace Test
{
    public class Strings
    {
        public void Remove(string text, int startIndex, int count)
        {
            var result1 = text.|Remove(startIndex)|(0);
            var result2 = text.|Remove(0, count)|(1);
        }
    }
}
------------------------------------------------
0: Use the range indexer.
QUICKFIXES:
Replace with '[..startIndex]'
1: Use the range indexer.
QUICKFIXES:
Replace with '[count..]'
