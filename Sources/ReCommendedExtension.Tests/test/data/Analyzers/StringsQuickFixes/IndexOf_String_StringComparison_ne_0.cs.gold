using System;

namespace Test
{
    public class Strings
    {
        public void Comparison(string text, string s)
        {
            var result = !text.StartsWith{caret}(s, StringComparison.CurrentCulture);
        }
    }
}