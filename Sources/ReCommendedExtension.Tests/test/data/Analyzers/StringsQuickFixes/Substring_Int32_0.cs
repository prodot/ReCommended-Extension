using System;

namespace Test
{
    public class Strings
    {
        public void Substring(string text, int startIndex)
        {
            var result = text.Sub{caret}string(startIndex, 0);
        }
    }
}