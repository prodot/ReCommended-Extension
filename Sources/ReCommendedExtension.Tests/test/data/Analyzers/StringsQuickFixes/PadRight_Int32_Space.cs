using System;

namespace Test
{
    public class Strings
    {
        public void Invocation(string text, int totalWidth)
        {
            var result = text.PadRight(totalWidth, ' '{caret});
        }
    }
}