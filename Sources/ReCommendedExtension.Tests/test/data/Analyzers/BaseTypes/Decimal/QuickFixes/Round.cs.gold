using System;

namespace Test
{
    public class Decimals
    {
        public void Round(decimal d, MidpointRounding mode)
        {
            var result = decimal.Round(d{caret}, mode);
        }
    }
}