using System;

namespace Test
{
    public class Decimals
    {
        public void ExpressionResult(decimal number)
        {
            var result11 = |decimal.Clamp(number, 1, 1)|(0);
            var result12 = |decimal.Clamp(number, decimal.MinValue, decimal.MaxValue)|(1);

            var result21 = |Math.Clamp(number, 1m, 1m)|(2);
            var result22 = |Math.Clamp(number, decimal.MinValue, decimal.MaxValue)|(3);
        }

        public void NoDetection(decimal number, decimal min, decimal max)
        {
            var result11 = decimal.Clamp(number, 1, max);
            var result12 = decimal.Clamp(number, min, 10);

            var result21 = Math.Clamp(number, 1m, max);
            var result22 = Math.Clamp(number, min, decimal.MaxValue);

            decimal.Clamp(number, 1, 1);
            decimal.Clamp(number, decimal.MinValue, decimal.MaxValue);

            Math.Clamp(number, 1m, 1m);
            Math.Clamp(number, decimal.MinValue, decimal.MaxValue);
        }
    }
}
---------------------------------------------------------
(0): ReSharper Suggestion: The expression is always 1.
(1): ReSharper Suggestion: The expression is always the same as the first argument.
(2): ReSharper Suggestion: The expression is always 1.
(3): ReSharper Suggestion: The expression is always the same as the first argument.
