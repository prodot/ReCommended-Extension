using System;
using System.Text;

namespace Test
{
    public class StringBuilders
    {
        public void Replace(StringBuilder builder, int startIndex, int count)
        {
            var result = builder.Replace('a'{caret}, 'b', startIndex, count);
        }
    }
}