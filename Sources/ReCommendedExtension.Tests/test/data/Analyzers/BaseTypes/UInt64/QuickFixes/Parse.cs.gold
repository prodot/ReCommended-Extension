using System;
using System.Globalization;

namespace Test
{
    public class UInt64s
    {
        public void Parse(string s, IFormatProvider provider)
        {
            var result = ulong.Parse(s{caret}, provider);
        }
    }
}