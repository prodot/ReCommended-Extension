using System;
using System.Globalization;

namespace Test
{
    public class TimesOnly
    {
        public void RedundantArgument(ReadOnlySpan<char> s, IFormatProvider? provider, out TimeOnly result)
        {
            var result1 = TimeOnly.TryParse(s, provider{caret}, out result);
        }
    }
}