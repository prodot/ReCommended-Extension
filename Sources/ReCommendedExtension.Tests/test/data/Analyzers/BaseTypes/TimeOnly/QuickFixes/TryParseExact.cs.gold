using System;
using System.Globalization;

namespace Test
{
    public class TimesOnly
    {
        public void TryParseExact(string s, string format, out TimeOnly result)
        {
            var result1 = TimeOnly.TryParseExact(s, format{caret}, out result);
        }
    }
}