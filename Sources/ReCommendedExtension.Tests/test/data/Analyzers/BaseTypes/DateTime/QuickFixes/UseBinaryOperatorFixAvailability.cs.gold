using System;

namespace Test
{
    public class DateTimes
    {
        public void Add(DateTime dateTime, TimeSpan value)
        {
            var result = |dateTime.Add(value)|(0);
        }

        public void Equals(DateTime dateTime, DateTime value)
        {
            var result1 = |dateTime.Equals(value)|(1);
            var result2 = |DateTime.Equals(dateTime, value)|(2);
        }

        public void Subtract(DateTime dateTime, DateTime dateTimeValue, TimeSpan timeSpanValue)
        {
            var result1 = |dateTime.Subtract(dateTimeValue)|(3);
            var result2 = |dateTime.Subtract(timeSpanValue)|(4);
        }
    }
}
------------------------------------------------
0: Use the '+' operator.
QUICKFIXES:
Replace with 'dateTime + value'
1: Use the '==' operator.
QUICKFIXES:
Replace with 'dateTime == value'
2: Use the '==' operator.
QUICKFIXES:
Replace with 'dateTime == value'
3: Use the '-' operator.
QUICKFIXES:
Replace with 'dateTime - dateTimeValue'
4: Use the '-' operator.
QUICKFIXES:
Replace with 'dateTime - timeSpanValue'
