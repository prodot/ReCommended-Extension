using System;

namespace Test
{
    public class Int64s
    {
        public void ExpressionResult(long number)
        {
            var result11 = |long.Clamp(number, 1, 1)|(0);
            var result12 = |long.Clamp(number, long.MinValue, long.MaxValue)|(1);

            var result21 = |Math.Clamp(number, 1L, 1L)|(2);
            var result22 = |Math.Clamp(number, long.MinValue, long.MaxValue)|(3);
        }

        public void NoDetection(long number, long min, long max)
        {
            var result11 = long.Clamp(number, 1, max);
            var result12 = long.Clamp(number, min, 10);

            var result21 = Math.Clamp(number, (long)1, max);
            var result22 = Math.Clamp(number, min, long.MaxValue);

            long.Clamp(number, 1, 1);
            long.Clamp(number, long.MinValue, long.MaxValue);

            Math.Clamp(number, 1L, 1L);
            Math.Clamp(number, long.MinValue, long.MaxValue);
        }
    }
}
---------------------------------------------------------
(0): ReSharper Suggestion: The expression is always 1.
(1): ReSharper Suggestion: The expression is always the same as the first argument.
(2): ReSharper Suggestion: The expression is always 1.
(3): ReSharper Suggestion: The expression is always the same as the first argument.
