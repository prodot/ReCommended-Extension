using System;

namespace Test
{
    public class TimeSpans
    {
        public void TryParseExact(string input, string format, IFormatProvider formatProvider, out TimeSpan result)
        {
            var result_ = TimeSpan.TryParseExact(input, form{caret}at, formatProvider: formatProvider, out result);
        }
    }
}