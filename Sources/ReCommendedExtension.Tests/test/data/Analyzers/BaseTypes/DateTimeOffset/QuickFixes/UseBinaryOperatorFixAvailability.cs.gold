using System;

namespace Test
{
    public class DateTimeOffsets
    {
        public void Add(DateTimeOffset dateTimeOffset, TimeSpan timeSpan)
        {
            var result = |dateTimeOffset.Add(timeSpan)|(0);
        }

        public void Equals(DateTimeOffset dateTimeOffset, DateTimeOffset other)
        {
            var result1 = |dateTimeOffset.Equals(other)|(1);
            var result2 = |DateTimeOffset.Equals(dateTimeOffset, other)|(2);
        }

        public void Subtract(DateTimeOffset dateTimeOffset, DateTimeOffset dateTimeOffsetValue, TimeSpan timeSpanValue)
        {
            var result1 = |dateTimeOffset.Subtract(dateTimeOffsetValue)|(3);
            var result2 = |dateTimeOffset.Subtract(timeSpanValue)|(4);
        }
    }
}
------------------------------------------------
0: Use the '+' operator.
QUICKFIXES:
Replace with 'dateTimeOffset + timeSpan'
1: Use the '==' operator.
QUICKFIXES:
Replace with 'dateTimeOffset == other'
2: Use the '==' operator.
QUICKFIXES:
Replace with 'dateTimeOffset == other'
3: Use the '-' operator.
QUICKFIXES:
Replace with 'dateTimeOffset - dateTimeOffsetValue'
4: Use the '-' operator.
QUICKFIXES:
Replace with 'dateTimeOffset - timeSpanValue'
