using System;

namespace Test
{
    public class DateTimeOffsets
    {
        public void Parse(string input)
        {
            var result = DateTimeOffset.Parse(input, n{caret}ull);
        }
    }
}