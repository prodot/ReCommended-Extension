using System;
using System.Globalization;

namespace Test
{
    public class DateTimeOffsets
    {
        public void ParseExact(string input, IFormatProvider formatProvider, DateTimeStyles styles)
        {
            var result = DateTimeOffset.ParseExact(input, ["r"{caret}], formatProvider, styles);
        }
    }
}