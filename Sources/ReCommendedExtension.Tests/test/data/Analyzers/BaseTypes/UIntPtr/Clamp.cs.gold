using System;

namespace Test
{
    public class UIntPtrs
    {
        public void ExpressionResult(nuint number)
        {
            var result1 = |nint.Clamp(number, 1, 1)|(0);

            var result2 = |Math.Clamp(number, (nuint)1, (nuint)1)|(1);
        }

        public void NoDetection(nuint number, nuint min, nuint max)
        {
            var result11 = nint.Clamp(number, 1, max);
            var result12 = nint.Clamp(number, min, 10);

            var result21 = Math.Clamp(number, (nuint)1, max);
            var result22 = Math.Clamp(number, min, 10);

            nint.Clamp(number, 1, 1);

            Math.Clamp(number, (nuint)1, (nuint)1);
        }
    }
}
---------------------------------------------------------
(0): ReSharper Suggestion: The expression is always 1.
(1): ReSharper Suggestion: The expression is always 1.
