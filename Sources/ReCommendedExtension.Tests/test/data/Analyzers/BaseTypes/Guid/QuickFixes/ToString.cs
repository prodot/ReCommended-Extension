using System;

namespace Test
{
    public class Guids
    {
        public void ToString(Guid guid, string format, IFormatProvider provider)
        {
            var result = guid.ToString(format, provider{caret});
        }
    }
}