using System;

namespace Test
{
    public class Guids
    {
        public void Parse(string s, ReadOnlySpan<char> s1, IFormatProvider provider)
        {
            var result1 = Guid.Parse(s, |provider|(0));
            var result2 = Guid.Parse(s1, |provider|(1));
        }

        public void TryParse(string s, ReadOnlySpan<char> s1, IFormatProvider provider, out Guid result)
        {
            var result1 = Guid.TryParse(s, |provider|(2), out result);
            var result2 = Guid.TryParse(s1, |provider|(3), out result);
        }
    }
}
------------------------------------------------
0: Passing a format provider is redundant.
QUICKFIXES:
Remove argument
1: Passing a format provider is redundant.
QUICKFIXES:
Remove argument
2: Passing a format provider is redundant.
QUICKFIXES:
Remove argument
3: Passing a format provider is redundant.
QUICKFIXES:
Remove argument
