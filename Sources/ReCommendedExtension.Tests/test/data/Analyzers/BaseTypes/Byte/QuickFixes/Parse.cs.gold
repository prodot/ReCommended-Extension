using System;
using System.Globalization;

namespace Test
{
    public class Bytes
    {
        public void Parse(string s, IFormatProvider provider)
        {
            var result = byte.Parse(s{caret}, provider);
        }
    }
}