using System;
using System.Globalization;

namespace Test
{
    public class DatesOnly
    {
        public void ParseExact(string s, ReadOnlySpan<char> s1, string format, IFormatProvider provider, DateTimeStyles style)
        {
            var result11 = DateOnly.ParseExact(s, "R", |provider|(0), style);
            var result12 = DateOnly.ParseExact(s, "R", |provider|(1));

            var result21 = DateOnly.ParseExact(s, |[format]|(2));

            var result31 = DateOnly.ParseExact(s, |[format]|(3), provider, style);
            var result32 = DateOnly.ParseExact(s, ["o", "r"], |provider|(4), style);

            var result41 = DateOnly.ParseExact(s1, ["o", "r"], |provider|(5), style);
        }

        public void TryParseExact(string s, ReadOnlySpan<char> s1, string format, IFormatProvider provider, DateTimeStyles style, out DateOnly result)
        {
            var result1 = DateOnly.TryParseExact(s, "R", |provider|(6), style, out result);
            var result2 = DateOnly.TryParseExact(s, |[format]|(7), out result);
            var result3 = DateOnly.TryParseExact(s, |[format]|(8), provider, style, out result);
            var result4 = DateOnly.TryParseExact(s, ["o", "r"], |provider|(9), style, out result);
            var result5 = DateOnly.TryParseExact(s1, ["o", "r"], |provider|(10), style, out result);
        }
    }
}
------------------------------------------------
0: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
1: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
2: The only collection element should be passed directly.
QUICKFIXES:
Replace with 'format'
3: The only collection element should be passed directly.
QUICKFIXES:
Replace with 'format'
4: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
5: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
6: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
7: The only collection element should be passed directly.
QUICKFIXES:
Replace with 'format'
8: The only collection element should be passed directly.
QUICKFIXES:
Replace with 'format'
9: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
10: The format provider is ignored (pass null instead).
QUICKFIXES:
Replace with 'null'
