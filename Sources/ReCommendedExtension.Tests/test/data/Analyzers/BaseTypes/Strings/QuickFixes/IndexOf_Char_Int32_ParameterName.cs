using System;

namespace Test
{
    public class Strings
    {
        public void IndexOf(string text, char c)
        {
            var result = text.IndexOf(c, startI{caret}ndex: 0);
        }
    }
}