using System;

namespace Test
{
    public class Strings
    {
        public void LastIndexOfAny(string text, char c, int startIndex, int count)
        {
            var result = text.LastIndex{caret}OfAny([c], startIndex, count);
        }
    }
}