namespace Test
{
    public class Strings
    {
        public void StartsWith(string text, char c)
        {
            var result = text is [var firstChar, ..] && f{caret}irstChar == c;
        }
    }
}