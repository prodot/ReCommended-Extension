using System;

namespace Test
{
    public class Strings
    {
        public void RangeIndexer(string text, int startIndex, int count)
        {
            var result = text.Remove{caret}(startIndex);
        }
    }
}