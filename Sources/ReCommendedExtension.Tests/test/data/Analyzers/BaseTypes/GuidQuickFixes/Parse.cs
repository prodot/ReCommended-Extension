using System;

namespace Test
{
    public class Guids
    {
        public void Parse(string s, IFormatProvider provider)
        {
            var result = Guid.Parse(s, provider{caret});
        }
    }
}