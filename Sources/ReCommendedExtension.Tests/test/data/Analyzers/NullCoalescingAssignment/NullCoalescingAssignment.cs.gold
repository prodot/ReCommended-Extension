using System;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace Tests
{
    public class Sample
    {
        void EqualityComparison(string value, string x)
        {
            |if|(0) (value == null)
            {
                value = "one";
            }

            |if|(1) (x == null) x = new Version(1, 2).ToString();
        }

        void EqualityComparison_Reverse(string value, string x)
        {
            |if|(2) (null == value)
            {
                value = string.Join(", ", new[] { "one", "two", "three" });
            }

            |if|(3) (null == x) x = "four".Remove(0, 1);
        }

        class Data
        {
            string Property { get; set; }
        }

        Data data;

        string Property { get; set; }

        void IsComparison(string fallbackValue)
        {
            |if|(4) (data.Property is null)
            {
                data.Property = fallbackValue;
            }

            |if|(5) (Property is null) Property = fallbackValue.Trim();
        }

        void Unavailable(string x)
        {
            if (value == null)
            {
                value = "one";
            }
            else
            {
                value = "two";
            }

            if (value == null)
            {
                value = "one";
                value = "two";
            }
        }
    }
}
---------------------------------------------------------
(0): ReSharper Suggestion: Replace 'if' statement with compound assignment.
(1): ReSharper Suggestion: Replace 'if' statement with compound assignment.
(2): ReSharper Suggestion: Replace 'if' statement with compound assignment.
(3): ReSharper Suggestion: Replace 'if' statement with compound assignment.
(4): ReSharper Suggestion: Replace 'if' statement with compound assignment.
(5): ReSharper Suggestion: Replace 'if' statement with compound assignment.
