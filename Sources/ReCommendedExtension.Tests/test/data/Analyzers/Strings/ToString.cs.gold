using System;

namespace Test
{
    public class Strings
    {
        public void RedundantInvocation(string text, IFormatProvider provider)
        {
            var result1 = text.|ToString(provider)|(0);
            var result2 = text.|ToString(null)|(1);
        }

        public void NoDetection(string text, int startIndex, int length)
        {
            text.ToString(provider);
            text.ToString(null);
        }
    }
}
---------------------------------------------------------
(0): ReSharper Dead Code: Invoking 'ToString' with a format provider is redundant.
(1): ReSharper Dead Code: Invoking 'ToString' with a format provider is redundant.
