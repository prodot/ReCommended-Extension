using System.Linq;

namespace Test
{
    public class Methods
    {
        public void Pattern(int[] array)
        {
            var result = array is [var item] ? item : throw new InvalidOperationException{caret}("List is either empty or contains more than one element.");
        }
    }
}