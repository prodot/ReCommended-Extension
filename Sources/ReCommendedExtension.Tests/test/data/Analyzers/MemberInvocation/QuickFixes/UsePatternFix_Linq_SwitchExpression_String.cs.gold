using System.Linq;

namespace Test
{
    public class Methods
    {
        public void SingleOrDefault(string text)
        {
            var result = text switch
            {
              [] => '\0',
              [var item] => item,
              _ => throw new InvalidOperationException("String contains more than one character."{caret})
            };
        }
    }
}